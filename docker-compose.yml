version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src/data:/app/src/data
      - chroma_db:/app/chroma_db
    restart: unless-stopped

volumes:
  chroma_db:
